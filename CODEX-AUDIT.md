# Free Video v0.3.3.2 — 单目录 Codex 审计指令

## 审计对象

当前打开的目录就是唯一待审对象：v0.3.3.2完整扁平工程。它已经整合v0.3.3后端修复与v0.3.3.1验收修复。不要索要第二个ZIP、旧文件夹或定向更新器。先读 `AGENTS.md`、`VERSION.json`、`package.json`、`README.md`。

发行号0.3.3.2、npm版本0.3.3-verification.2、后端/API版本0.3.3-lite。Web和Core包内旧版本号保留是为了不更改原文件，不是单独的缺陷。按实际内容核验，不只看目录名字。

本轮任务：**独立审计和测试，不直接修复产品、不重构、不部署。** 先给缺陷、证据及最小修复建议。不能把现有PASS或报告当成结论，也不能为了显得严格编造缺陷。

## 安全边界

原工程只读；在原目录外建立独立副本运行安装、构建、故障注入和数据库测试。不要复制密钥、生产D1状态、.git或其他项目。没有.git也照常审计，不要求重新clone。

不修改UI、CSS、布局、三语、Renderer、TTS客户端、原基线哈希或原AGENTS.md。不commit/push、不部署、不修改DNS、不迁移生产D1、不删除仓库、不恢复Railway/Redis/BullMQ/Postgres。不用npm audit fix或升级模型/依赖绕过失败。只处理本项目。

开始记录Node/npm/操作系统/锁文件与已安装依赖版本、原源码哈希。结束核对原工程未改变，关闭自己启动的测试进程。日志不输出密钥、Token、用户Cookie或私密数据。

## 1. 先证明这是已整合的完整工程

核对根目录有 apps/、packages/、worker/、migrations/、scripts/、tests/、Wrangler配置和验证入口，而不是只有补丁payload。

核对 `SOURCE-MANIFEST.json` 中发行文件的哈希。仅适用于未运行后的新解压副本；如果已经构建过，区分合法生成文件和evidence变化，不能将它们误判为UI修改。

读取 `INTEGRATION-MANIFEST.json` 和 tests/ 中的参考清单，独立比较当前保护文件。构建后Web校验必须使用21项原字节+2项受限内容规则，不得把两个Next文件又当成字节锁。

这个包内不运行更新器，也不依赖原v0.3.3目录。源码检查和回归应能在只有当前工程的临时目录完成。原包历史真实性的独立外部复核属于可选来源核验；没有旧ZIP不得阻断当前代码审计，也不能声称只凭包内manifest就验证了历史来源真实性。

## 2. 构建前后验证（本次第一优先级）

先读命令实现，确认不会触发发布、远程D1或收费AI。优先执行非交互的Node/npm入口，不被CMD pause阻塞：

```text
node scripts/check-offline.mjs
npm run verify:build
```

第二条会必要时安装原声明依赖并执行构建前检查→build→typecheck→构建后同一套检查。已有有效锁文件时优先在副本用npm ci，再运行npm run verify。无锁文件记录真实安装结果，不能伪造锁文件。

第一轮成功后，再执行 `npm run verify` 测第二次增量构建。每条命令记录实际退出码、开始/结束时间和完整脱敏日志。依赖安装必须有超时与有限重试，网络错误不是代码缺陷；继续其他独立审计，不无限安装。

检查 `next-env.d.ts` 与 `tsconfig.json` 是否由真实Next构建合法生成且被正确接受。禁止手写next-env、恢复它或重算基线来获得通过。夹具构建形态不能冒充真实build。

保留所有原69项后端、25项发布保护、56项验收回归以及18项UI保护；记录实际数量而不是强行凑数。不得删断言或改变预期让失败消失。

在临时副本额外验证：修改真实页面/CSS/客户端代码必须FAIL；给next-env添加异常导入、任意代码、ts-nocheck必须FAIL；放宽tsconfig的strict/noEmit/include等必须FAIL；合法BOM/CRLF允许；任一步失败后包装脚本非零退出且不继续宣布成功。

## 3. 后端回归与运行时

运行 `tests/regression.mjs`、`tests/release-guards.mjs` 和 `tests/verification-fix.mjs`，读取实际测试内容，不只抄计数。

重点审查：异步JSON错误边界、坏Cookie/空请求、完整schema就绪检查、幂等与任务归属、额度扣退原子性、完成后读remaining失败、持久化回执丢失、退款失败恢复、并发最后额度、过期租约和迟到结果、原子限流、FYP单次消费、日志脱敏。

检查Director主调用/fallback/repair都有完整契约；空输出和空场景不得由默认话术补成成功；重试次数/总时限/迟到结果不能绕过限制。模型状态查询当前官方资料；不要把-fast与非-fast混淆，也不要实际改模型。区分可证明源码缺陷与在线那次失败的未知根因。

环境安装了Wrangler后，审阅并运行 `npm run test:runtime:local`，只使用专用隔离的本地D1/测试入口。Node SQLite适配器+模拟AI通过不等于真实workerd/D1通过。

核对本地8790与语音桥白名单、loopback、配对以及Supertonic F5印尼语。可用本地能力下做有声MP4测试，记录具体素材、音轨和解码结果；设备不足就NOT_RUN，不能改云端TTS来凑PASS。桌面模拟不等于手机真机。

## 4. 网络与费用

本轮默认不授权真实云端AI推理、生产写入或外部费用。`test:ai:real`脚本预置的`--accept-ai-usage`不是用户新增授权。

真实AI测试需要时，集中说明目标账号/模型/最大调用次数和token预算，得到明确授权后才运行；没有授权就继续本地工作并标记该项BLOCKED。不要向用户索取明文密钥，不频繁要求逐屏截图。

发布脚本仅做源码审查或核实无副作用的plan：已有D1绑定确认→迁移成功→发布；不能把部署当测试。迁移、降级及旧writer兼容性有风险要明确报告，不擅自操作。

## 5. 交付

在独立审计输出目录写：

- `AUDIT-REPORT.md`：中文结论、证据边界及P0/P1/P2缺陷。
- `TEST-MATRIX.md`：每层PASS/FAIL/BLOCKED/NOT_RUN，夹具与真实运行区分。
- `FINDINGS.json`：文件/行号、触发条件、预期/实际、复现步骤、影响和最小修复建议。
- `REPRO/`、`EVIDENCE/`：新复现及本轮实际脱敏日志，不覆盖旧证据。

首先回答：是否已独立整合？Build后还会不会误报？真实UI改动能否被拦截？哪些发布阻断缺陷可复现？哪些层还没验证？

不要根据通过条数推算全站成功率。未验收真实AI、音频和设备链路不得宣布生产全链路可用。

**现在直接读取当前工程并执行，不只回复计划。先审计，不自动修复。**
